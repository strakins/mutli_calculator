import 'package:flutter/material.dart';

class FurureFundsPage extends StatelessWidget {
  const FurureFundsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Furure Funds Calculator Page"),
      ),
    );
  }
}