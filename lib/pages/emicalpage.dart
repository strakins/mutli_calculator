import 'package:flutter/material.dart';

class EMIPAGE extends StatelessWidget {
  const EMIPAGE({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("EMI Funds Calculator Page"),
      ),
    );
  }
}