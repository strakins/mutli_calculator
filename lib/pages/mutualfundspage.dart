import 'package:flutter/material.dart';

class MutualFundsCalculatorPage extends StatelessWidget {
  const MutualFundsCalculatorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mutual Funds Calculator Page"),
      ),
    );
  }
}